import { SeachByExpensePipe } from './seach-by-expense.pipe';

describe('SeachByExpensePipe', () => {
  it('create an instance', () => {
    const pipe = new SeachByExpensePipe();
    expect(pipe).toBeTruthy();
  });
});
